# 🎯 ПРОСТАЯ ИНСТРУКЦИЯ - ЧТО ДЕЛАТЬ ДАЛЬШЕ

## ✅ Что уже сделано:
- Docker образ собран ✅
- Образ загружен в GitHub ✅
- Образ готов к использованию ✅

## 📋 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС (3 простых шага):

---

### ШАГ 1: Получить GitHub Token (2 минуты)

1. Откройте в браузере: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. В поле **"Note"** напишите: `Docker`
4. Внизу поставьте галочку на **"read:packages"**
5. Нажмите **"Generate token"** внизу страницы
6. **СКОПИРУЙТЕ ТОКЕН** (он показывается только один раз!)

---

### ШАГ 2: Войти в GitHub Container Registry (1 команда)

Откройте терминал и выполните:

```bash
echo "ВСТАВЬТЕ_ВАШ_ТОКЕН_СЮДА" | docker login ghcr.io -u Fek1r --password-stdin
```

**Замените `ВСТАВЬТЕ_ВАШ_ТОКЕН_СЮДА` на токен из Шага 1**

Например, если ваш токен `ghp_abc123xyz`, то команда будет:
```bash
echo "ghp_abc123xyz" | docker login ghcr.io -u Fek1r --password-stdin
```

Должно появиться: `Login Succeeded` ✅

---

### ШАГ 3: Запустить контейнер (3 команды)

Выполните эти команды по очереди:

```bash
# Команда 1: Создать файл с настройками
cat > .env << 'EOF'
POSTGRES_CONNECTION_STRING=Host=postgres;Port=5432;Database=postgres;Username=fek1r;Password=10021711
CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoiYWEyZjdmYzVkZTAwNGU1NmU0YzAxODllZGJkZDZlZDciLCJ0IjoiODU5NjFiM2ItYjQ4Mi00NTU4LWFjNmUtZTFmNDA5NDhiMzcyIiwicyI6Ik1qUm1OVEV4TVRrdE56WTBZaTAwT0dJNExXSmxNbUV0T0RBMFpUVXdPVEJsTURSayJ9
FRONTEND_URL=https://oxygen-sensor-frontend.pages.dev
POSTGRES_PASSWORD=10021711
EOF

# Команда 2: Запустить контейнеры
docker-compose -f docker-compose.prod.yml up -d

# Команда 3: Проверить что всё работает
docker ps
```

---

## ✅ ПРОВЕРКА

После выполнения команд, проверьте:

```bash
# Посмотреть логи
docker logs oxygen-sensor-backend

# Проверить API
curl http://localhost:8080/sensor/all
```

Если видите данные - всё работает! 🎉

---

## ❓ ЧТО ЕСЛИ НЕ РАБОТАЕТ?

### Ошибка "unauthorized"?
- Проверьте, что токен правильный
- Убедитесь, что поставили галочку на "read:packages"

### Ошибка "No such image"?
- Убедитесь, что вы вошли в GitHub (Шаг 2)
- Попробуйте: `docker pull ghcr.io/Fek1r/Oxygen-Sensor-Project/backend:latest`

### Контейнер не запускается?
- Посмотрите логи: `docker logs oxygen-sensor-backend`
- Проверьте, что порт 8080 свободен

---

## 🎯 ИТОГО:

1. Получить токен на GitHub (2 минуты)
2. Войти командой `docker login` (1 команда)
3. Запустить `docker-compose up -d` (1 команда)

**ВСЁ!** После этого ваш бэкенд будет работать на `http://localhost:8080`
